/* Browser-only cache. This is NOT a Cloudflare Worker or server-side function. */
'use strict';
const CACHE='995-reborn-gp024-altmerge';
const SHELL=["./index.html", "./manifest.webmanifest", "./assets/icon-192.png", "./assets/icon-512.png", "./assets/vehicles/jetta-mkiv/volkswagen-bora-jetta-mk4-2005.cc-by-4.0.glb", "./assets/vehicles/jetta-mkiv/CREDITS.md", "./assets/worlds/north-berwick/world.json", "./assets/worlds/north-berwick/CREDITS.md", "./assets/worlds/north-berwick/provenance.json", "./assets/worlds/north-berwick/facades-atlas.webp", "./assets/worlds/north-berwick/facades-atlas.json"];
self.addEventListener('install',event=>event.waitUntil((async()=>{const cache=await caches.open(CACHE);await cache.addAll(SHELL.map(path=>new Request(path,{cache:'reload'})));await self.skipWaiting();})()));
self.addEventListener('activate',event=>event.waitUntil((async()=>{for(const key of await caches.keys())if(key.startsWith('995-reborn-')&&key!==CACHE)await caches.delete(key);await self.clients.claim();})()));
self.addEventListener('fetch',event=>{const request=event.request,url=new URL(request.url);if(request.method!=='GET'||url.origin!==location.origin)return;if(request.mode==='navigate'){event.respondWith((async()=>{try{const response=await fetch(request);if(response.ok){const cache=await caches.open(CACHE);await cache.put('./index.html',response.clone());}return response;}catch(error){return await caches.match('./index.html')||Promise.reject(error);}})());return;}event.respondWith((async()=>{const cached=await caches.match(request);if(cached)return cached;const response=await fetch(request);if(response.ok&&response.type!=='opaque'&&SHELL.some(path=>new URL(path,self.registration.scope).href===url.href)){const cache=await caches.open(CACHE);await cache.put(request,response.clone());}return response;})());});
